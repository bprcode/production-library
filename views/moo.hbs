<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link rel="icon" href="/favicon.svg">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Voltaire&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="/clean-pastel.css">

  <title>{{title}}</title>
</head>

<body>
  <div class="page-container">
    <header>
      {{#> (lookup . 'partial_header') }}
        Failover header partial
      {{/undefined}}
    </header>

    <div class="ambient-bg"></div>
    <div class="center-content">
    <h1>{{title}}</h1>
    <p>Why does a 1px border fix everything? Why, CSS?</p>
    {{>(lookup . 'partial_name')}}
    {{#each animals}}<div>{{this}}</div>{{/each}}
    </div>

    <footer>
      {{#> (lookup . 'partial_footer') }}
        Failover footer partial
      {{/undefined}}
    </footer>
  </div>
</body>
</html>
